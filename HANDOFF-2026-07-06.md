# HANDOFF — 2026-07-06 · Stage 1 Close-Out build (Code.gs v6.3 → v6.4)

Boss — everything in the spec is coded, offline-tested (31/31 assertions), and packaged. Nothing has touched your live Sheet or deployment; this container can't reach Google. This note is the complete list of what YOU must run to close the loop.

## What shipped in this build

- **Bug 1 — due-date landmine:** RECURRING rows now get a cadence dropdown (DAILY / WEEKLY / FORTNIGHTLY / MONTHLY / 3X_WEEKLY), MILESTONE rows a date picker. Type-flip swaps the cell live. Backend refuses mismatches before writing anything.
- **Bug 2 — task IDs:** manual ID entry removed. Backend autogenerates next ID per domain under a script lock (HG-series correctly skips to HG8 even with HG3.1–3.4 present). Domain is now the required field on add.
- **Bug 3 — points_late:** defaults to the live `=ROUND(H*0.75,0)` formula; goes static only when you deliberately type a value; clearing the field restores the formula. `repairPointsLateFormulas_()` relinks historical frozen rows (value-preserving — anything not exactly 75% is logged as an intentional override and left alone).
- **Bug 4 — decay UI:** Decay Trigger + Decay/Day columns added to the desktop task table, editable per row.
- **Fork 5 — decided: Option B.** Stat-wide N3 net stays as-is; new per-task weekly decay columns (STAT_HISTORY AA/AB) charge −3 per missed HG3.x per calendar week, immune to unrelated STR activity. `migrateHG3Split_()` + `installPerTaskWeeklyDecayColumns_()` do the work. If you disagree with B, don't run those two functions — everything else is independent.
- **Fork 6 — dissolved, not decided.** The flat "1.7 L" water target was `=B9` — it reads your HEIGHT as litres. Weight-based target (≈2.74 L @ 83 kg) is now served by the API; both dashboards hydrate from config.
- **Bonus fix:** task-handler errors were being silently converted to `{"status":"ok"}` by the response wrapper. Now surfaced properly, and both frontends display them.
- **Backlog closed:** api.js retries reads once on transient failures. Writes are deliberately never retried (an ambiguous add_task retry could double-write with a fresh auto-ID).

## UNVERIFIED — your runbook (in order, stop on first failure)

1. **Deploy:** push repo to Pages; Apps Script → Deploy → Manage deployments → pencil → New version. **Edit the existing deployment — a new one rotates the /exec URL.**
2. **`testStage1CloseOut()`** from the Apps Script editor (runs all Stage-1 tests; creates and cleans up its own rows). Read the full log.
3. **True concurrency check (DoD 1):** fire two `add_task` curls in parallel (`&`) — both must land with distinct sequential IDs. The editor can't simulate this.
4. **`repairPointsLateFormulas_()`** — review its override log before moving on.
5. **`migrateHG3Split_()`** then **`installPerTaskWeeklyDecayColumns_()`** — watch the log for a `MANUAL STEP` line; it means the C3/D3 formula shape drifted from what I saw in the xlsx snapshot and you need to paste one suffix by hand (the log gives the exact string).
6. **Regression:** `testPing`, `testHAELocal`, `testWorkoutLocal`; then rename TASKS_MASTER temporarily and confirm the dashboard degrades to the task-panel empty-state without nuking telemetry/water; rename back.
7. **Reward toast:** complete a real task from the dashboard, confirm the toast shows the awarded points. Last outstanding Stage-1 item.
8. **Visual pass:** both themes, desktop + phone. Playwright screenshots were impossible here (browser binaries blocked by the network allowlist) — jsdom behavioral tests substituted, but nobody has *looked* at the two new columns at 375 px width.

## Still open after this build

- `repairAlcoholSessionFormulas_()` — never confirmed run; ALCOHOL_SESSIONS I:P chain unknown.
- Optional CONFIG cleanup: delete/repoint the `Water Daily Target (L)` row (the broken `=B9`).

## Deliverables

- `builder-os-dashboard-v6.4.zip` — full patched repo incl. new `CONTEXT.md`
- `Builder_OS_User_Guide_v1.1.docx` — §7 limitations updated (retry built, water resolved)
- `CONTEXT.md` — also standalone, for the vault
